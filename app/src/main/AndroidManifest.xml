<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="com.android.launcher.permission.INSTALL_SHORTCUT" />

    <application
        android:name=".TapwebApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tapweb">

        <activity
            android:name=".ui.home.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.addsite.AddSiteActivity"
            android:label="@string/add_site" />

        <activity
            android:name=".ui.settings.SettingsActivity"
            android:label="@string/settings" />

        <!-- Callback receiver for pinned shortcut confirmation.
             Receives explicit broadcast via PendingIntent, so it works on all Android versions. -->
        <receiver
            android:name=".shortcut.ShortcutPinReceiver"
            android:exported="false" />

        <!-- Each website gets its own task; same website reuses existing task -->
        <activity
            android:name=".ui.webview.WebViewActivity"
            android:configChanges="orientation|screenSize|keyboard|keyboardHidden"
            android:documentLaunchMode="intoExisting"
            android:exported="true"
            android:autoRemoveFromRecents="true">
            <intent-filter>
                <action android:name="com.tapweb.action.OPEN_WEBVIEW" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

    </application>

</manifest>
